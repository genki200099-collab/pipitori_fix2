# ピピトリ v33 シュート・ザ・ピッグ回数設定 監査記録

作成日: 2026-07-24
基準: v32

## 変更目的

シュート・ザ・ピッグの使用回数を次の2方式から選べるようにする。

- **無制限（標準・デフォルト）**: 同じプレイヤーが別ラウンドで再び条件を満たした場合、何度でも発動できる。
- **1人1回まで**: 各プレイヤーは1ゲームにつき最大1回だけ発動できる。

同一ラウンド中は、状態再送や重複処理があっても1回だけ判定・加点する既存の冪等性を維持する。

## 発見した重要な矛盾と修正

### P1: 過去ラウンドのシュートが後続ラウンドのマッド失点まで免除する経路

旧実装にはプレイヤーへ永続的に残る免除フラグがあり、一度シュートを発動すると、その後のラウンドでシュートが成立していなくてもマッド・ピッグ失点を免除し得る経路があった。

修正後は、免除を**そのラウンドのシュート判定結果だけ**から算出する。

- シュートが成立したラウンド: そのラウンドのババブタ・マッド失点を0にする。
- 後続ラウンドで不成立: 通常どおり失点する。
- 全シャッフル／持ち越しの両方式で、確定済みラウンド得点を二重計算しない。

### P2: UI設定とサーバー判定の不一致リスク

回数設定をクライアント表示だけにせず、部屋状態としてサーバーが保持・正規化するようにした。旧クライアントや欠落値は安全に `unlimited` として扱う。

## サーバー処理

- `shootThePigLimit`: `unlimited` / `once`
- 正規化関数を追加し、未知の値は `unlimited` へフォールバック。
- 発動可否はON/OFF、所持条件、ラウンド内判定済み、回数上限をサーバーで一元判定。
- `unlimited`: 同じプレイヤーの別ラウンド再発動を許可。
- `once`: そのプレイヤーの累計発動数が1以上なら再発動を拒否。
- 他プレイヤーの1回権は独立して維持。
- 各発動で他3人への-10点を累積。
- 再戦時は発動回数とラウンド別結果を初期化。

## CPU処理

- CPUがシュート成立を守る判断でも、選択中の回数制限を参照。
- `once`で既に権利を使ったCPUは、シュート成立を前提とする保持判断をしない。
- `unlimited`では、後続ラウンドでも再び成立を狙える。

## UI・説明文

部屋作成画面へ選択欄を追加。

- 無制限（標準）
- 1人1回まで

整合性を更新した箇所:

- 標準プリセット
- 各プリセット
- ルールヘルプ
- ルール早見表示
- 試合傾向
- 対戦中ルール表示
- プレイヤーHUD
- ラウンド結果
- シュート専用演出
- 最終結果
- README

表示例:

- 無制限: `🌕×2`、`無制限・2回目の発動`
- 1回まで: `🌕済`、`1人1ゲーム1回まで`

マッド・ピッグをOFFにした場合は、従来どおりシュートもOFFとなり、回数選択も操作不可にする。

## ババブタ失点タイミングとの関係

- **ラウンドごと**: 無制限なら複数ラウンドで再発動する意味がある。
- **ゲーム終了時**: シュート判定も最終ラウンドだけになるため、無制限と1回までの実質的な差はない。この組合せは説明文で明示する。

## 回帰テスト

### 自動テスト

`npm test` 全項目合格。

追加・強化した検査:

- デフォルトが無制限
- 未知値・欠落値が無制限へ正規化
- 同じプレイヤーが別ラウンドで2回発動可能
- 1人1回設定では2回目を拒否
- 他プレイヤーは自分の1回権を使用可能
- 1回の発動で他3人へ各-10点
- 複数回発動で-10点が累積
- 過去の発動が後続ラウンドのマッド／ババ失点を免除しない
- 数字分失点モードでも、成立ラウンドだけマッド-40を免除
- 全シャッフル／持ち越しの両方式
- 再戦時の初期化
- public stateと旧互換フィールド

ゲームエンジンストレス:

- 3ラウンド × 16ゲーム
- 全シャッフル／持ち越し
- 無制限／1人1回
- カード重複、非数得点、進行停止なし

### 実ブラウザ検査

- 設定専用: 3画面サイズ
  - 320×568
  - 568×320
  - 1280×720
- 重要レスポンシブ画面: 18ケース
- 横はみ出しなし
- 標準プリセットで無制限へ復帰
- ON/OFF依存関係が正常
- HUDの累計表示が正常

## 変更ファイル

- `server.js`
- `public/index.html`
- `README.md`
- `package.json`
- `tests/default_rules_regression.js`
- `tests/shoot_limit_regression.js`（新規）
- `tests/game_engine_stress_regression.js`
- `tests/websocket_game_smoke.js`
- `tests/browser_shoot_limit_v33.py`（新規）
- レスポンシブテスト用fixture各種

## 変更していないもの

- シュート成立条件
- 他3人へ各-10点
- 成立者の対象失点を0にする効果
- 1ラウンドにつき1回だけ判定する仕様
- マストフォロー、ピック、ペア浄化、得点体系、終了条件
