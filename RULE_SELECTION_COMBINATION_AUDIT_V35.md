# ピピトリ 選択ルール・組み合わせ整合性監査 v35

## 監査対象

v34を基準に、部屋作成時に選択可能な全設定について、次の層を横断して照合した。

- 画面上の初期値、入力欄、プリセット
- クライアント側の設定要約、ルール表示、試合傾向、HUD
- サーバー側の正規化と部屋設定
- CPU判断
- トリック、ピック、ペア浄化、得点、ラウンド終了、最終結果
- 再戦時の設定引き継ぎ
- スマホ縦・横、PCのレスポンシブ表示

## 最新の標準設定

- ラウンド数：3
- 次ラウンド：全カード回収・再シャッフル
- 手札失点：リンゴ・トウモロコシ・キャベツは1枚-3点、通常の水は1枚-6点
- マッド・ピッグ：使用する
- ババブタ失点：-20点
- ババブタ失点判定：ラウンドごと
- シュート・ザ・ピッグ：使用する
- シュート回数：無制限
- ピック候補：2枚
- 3枚パス：なし
- 開始時ペア捨て：なし

## 発見・修正した問題

### P1: マッド・ピッグOFFからONへ戻すとシュート設定が失われる

旧UIではマッド・ピッグをOFFにするとシュートをOFFへ強制変更し、その後マッドをONへ戻しても以前の選択を復元しなかった。

修正後は、依存関係による一時的な無効化と、ユーザーが明示的に選んだOFFを区別する。プリセット適用時はプリセット値を優先する。

### P1: 最終ラウンド判定＋全シャッフル時のCPU判断

ババブタ失点をゲーム最後のみ判定し、次ラウンドで全シャッフルする設定では、最終ラウンド前にババブタ＋マッドを保持しても次ラウンドへ持ち越せない。それにもかかわらずCPUがシュート候補として保持する経路があった。

修正後は次の通り。

- 各ラウンド判定：現在ラウンドで成立可能なら保持判断
- 最終ラウンド判定＋持ち越し：将来の成立を考慮可能
- 最終ラウンド判定＋全シャッフル：最終ラウンド前は保持対象にしない

### P2: 設定の実効状態が要約表示から分かりにくい

マッドOFF時のシュート使用不可や、シュートOFF時の回数設定無効が、コンパクト表示で判別しづらかった。

以下を明示するようにした。

- マッド・ピッグ：使用／不使用
- シュート：無制限／1人1回／不使用／マッドOFFのため使用不可
- ババブタ失点がゲーム最後のみの場合：最終ラウンド判定

### P2: 0点設定が「-0」と表示される

ババブタ失点を0にすると、要約や部屋設定表示で「-0」となる経路があった。0点は常に「0」と表示する共通処理へ修正した。

### P2: カード点失点時のマッド内訳が曖昧

カード点失点では、手札中のマッド・ピッグは手札失点へ含まれ、ごちそう山のマッドだけが別項目になる。旧表示の「マッド・ピッグ失点」では二重計上のように見えた。

修正後は次の表示にした。

- カード点失点：`マッド山失点`／最終表は`マッド山`
- その他の失点方式：`マッド固有失点`
- シュートによる他者失点：`シュート失点（累計）`

## 組み合わせ検証

### サーバー・表示ロジック全組み合わせ

18,432通りを検証。

対象軸：

- 4種類の手札失点方式
- マッドON/OFF
- ババブタ失点タイミング2種類
- シュートON/OFF
- 配り方2種類
- シュート回数2種類
- ピック候補0〜13
- 3枚パスON/OFF
- 開始時ペア捨てON/OFF

確認項目：

- 入力正規化
- 実効シュート設定
- 部屋設定要約
- 得点の有限性
- `undefined`、`NaN`、`[object Object]`、`-0`がない
- シュート回数制限と失点累積
- 最終ラウンド判定
- CPUのシュート保持判断

結果：18,432 / 18,432 合格。

### 実ゲーム進行マトリクス

384ゲームを実行し、すべて1ラウンド完走。

- 4失点方式
- マッドON/OFF
- 失点タイミング2種類
- シュートON/OFF
- 配り方2種類
- 回数制限2種類
- ピック候補0／2／13
- パス・開始時ペアの組み合わせ

結果：進行停止、カードID重複、非数得点なし。

### 実ブラウザUIマトリクス

1,152ケースをChromiumで描画。

- スマホ縦
- スマホ横
- PC
- 各設定の依存関係、要約、最終説明、ラベル

結果：失敗0、ページ横はみ出し0。

加えて、重要レスポンシブ18ケース、9フェイズ×6画面サイズの54ケース、最終結果コントラスト5画面サイズも合格。

## ルール変更の有無

ゲームルール自体は変更していない。既存の選択ルールを正しく解釈・表示し、CPU判断と得点内訳の整合性を修正した。

## 変更ファイル

- `server.js`
- `public/index.html`
- `package.json`
- `tests/rule_combination_matrix_regression.js`
- `tests/rule_gameplay_matrix_regression.js`
- `tests/browser_rule_matrix_v35.py`
- 本監査記録および結果JSON
