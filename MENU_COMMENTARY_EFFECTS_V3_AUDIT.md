# メニュー・実況・局面演出 v3 検証記録

作成日：2026-07-18 JST

## 改修内容

- ロビーを名前、作成・参加、プリセット、詳細設定の順へ再構成。
- 4種類のルールプリセットとカスタム自動判定を追加。
- CPUキャスト紹介を追加。
- CPUコメントへ局面・感情・強度・アイコン・ラベルを付与。
- ラウンド開始、通常手、危険札、終盤、勝者、最弱、候補選択、ピック、ペア、ババブタ、シュート、終了へ専用表現を追加。
- 操作を妨げない卓面背景エフェクトと状態別トーストを追加。
- スマホではプリセットとCPU紹介を横スワイプにし、実況を縦3行・横2行へ制限。

## 構文・DOM検証

- `server.js`：Node.js構文確認済み。
- クライアントJavaScript：Acorn解析成功。
- CSS：CSSTree解析成功。
- HTML：parse5解析エラー0件。
- DOM ID：58個、重複0件。
- 作成、参加、4プリセット、CPU3人、ゲーム画面、実況、局面エフェクトの必須要素を確認。

## 動的UI検証

- ドラマチック選択時にババブタ-30、ピック候補6枚を反映。
- 個別変更時にカスタム表示へ切り替え。
- スタンダードへ戻すと3ラウンド、ピック2枚、シュートONへ復帰。
- ババブタコメントで `mood-shock`、`intensity-critical`、`event-baba`、アイコン、ラベルを描画。
- 局面エフェクトの danger / pair / review / turn を動的描画。
- サーバー実配信でCPU3人の `cpuTitle` / `cpuStyle` と、参戦・ラウンド開始コメントのメタデータを確認。

## レスポンシブ確認

- メニューは900px以下でプリセット2列、640px以下で横スワイプへ切り替え。
- 作成・参加は640px以下で1列、参加入力は360px以下で縦積み。
- CPU紹介もスマホで横スワイプ。
- 実況最大幅はPC 330px、390px幅で142px、320px幅で108px、横667pxで124px。いずれも画面幅45%未満。
- 卓面中央カードは以下の全ケースで計算上の卓内収まりを確認。
  - 1366×768
  - 1024×768
  - 1024×600
  - 390×844
  - 320×568
  - 844×390
  - 667×375
- safe-area、`100dvh`、`prefers-reduced-motion`、エフェクトの `pointer-events:none` を確認。

## WebSocket実進行検証

- 1人＋CPU3人、1ラウンドを87.435秒で正常完走。
- 通常プレイ7回、プレイヤーピック2回、ペア判断2回を実行。
- lobby → playing → finished を通過。
- 最終得点：-16 / -20 / 0 / -32。全員の得点が数値として生成された。
- 実際に観測した実況イベント：greeting、round-start、card-play、watch、trick-weak、target-select、trick-win、pick、endgame、pair、finish。
- 全コメントで局面メタデータが欠けていないことを確認。
- サーバー標準エラー出力なし。
- 3枚パス＋開始時ペア捨て＋スペード失点モードでも passing → initialPair → playing を確認。

## 検証環境上の制約

- クラウドブラウザからローカルURLを表示できない制約があるため、スクリーンショットによるピクセル比較は未実施。
- 代替として構文解析、DOM契約、動的関数単体テスト、寸法計算、HTTP配信、WebSocket実進行を実施した。
